# 画像のサイズ・回り込み・キャプション

DBの問題・解答・解説本文にある画像記法の直後へ属性タグを付けます。画像の再アップロードやDBの項目追加は不要です。

```text
![図](/api/image/example.png){size=medium align=right caption="図1 年齢別の割合"}
```

- `size`: `large`（75％）、`medium`（50％）、`small`（25％）、`full`（100％）。本文幅に対する割合です。
- `align`: `left`（画像を左・本文を右へ回り込み）、`right`（画像を右・本文を左へ回り込み）、`center`（中央・回り込みなし）。
- `caption`: 二重引用符で囲んだ文字を画像の下に表示します。改行は入れず、引用符は `\"`、バックスラッシュは `\\` と記述します。HTMLは文字として表示します。

各属性は省略でき、順序も自由です。配置省略は中央。サイズなしで左右回り込みを指定すると中サイズ。キャプションのみなら元の画像幅を上限として表示します。タグのない既存画像は従来通り表示します。

画面幅768px以下では、左右回り込みを指定した画像をキャプション付きの全幅表示に切り替えます。印刷では指定サイズ・配置を使います。回り込みは同一の本文・設問・解答などの表示領域内に収まり、見出し・区切り線・語注で解除されます。

キャプション・属性・画像URLは本文の英語語数から除外します。既存DBの変更には通常の監査・差分確認付き修正手順を使います。
